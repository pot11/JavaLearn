spring:
  datasource:
    url: jdbc:mysql://localhost:3306/worker_management?useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: Aa17902094033.
    driver-class-name: com.mysql.cj.jdbc.Driver
    type: com.zaxxer.hikari.HikariDataSource
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000

  # 静态资源配置
  web:
    resources:
      static-locations: classpath:/static/

  # MVC配置
  mvc:
    static-path-pattern: /static/**

server:
  port: 8080

# MyBatis Plus 配置
mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    cache-enabled: true
    lazy-loading-enabled: true
    multiple-result-sets-enabled: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl  # 打印SQL日志
  global-config:
    db-config:
      id-type: auto
      logic-delete-field: deleted  # 全局逻辑删除字段名
      logic-delete-value: 1        # 逻辑已删除值
      logic-not-delete-value: 0    # 逻辑未删除值
      insert-strategy: not_null    # 插入策略
      update-strategy: not_null    # 更新策略
    banner: false  # 关闭MyBatis Plus的banner

# 日志配置
logging:
  level:
    com.example.workersalary.mapper: debug  # 设置mapper接口的日志级别
